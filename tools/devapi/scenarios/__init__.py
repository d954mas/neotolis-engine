"""tools.devapi.scenarios — runnable example workflows driving DevApiClient.

Each scenario connects to a running examples/devapi_host over loopback TCP and
demonstrates a bot-driven workflow. Stdlib only (socket + json), no pip deps.
"""
