# 22. FTUE production script

Цель: сделать FTUE настолько конкретным, чтобы по этому документу можно было реализовать первый playable без дополнительных устных объяснений.

Этот документ является главным рабочим сценарием FTUE. Если он конфликтует с более старыми FTUE-заметками, приоритет у этого документа.

Связанный анализ улучшений: [23_ftue_analysis_and_improvement_plan.md](23_ftue_analysis_and_improvement_plan.md).

Нарративный baseline для Step 1: [26_ftue_step_1_narrative.md](26_ftue_step_1_narrative.md). Если текст первых 6 секунд конфликтует с этим production-script, приоритет у `26_ftue_step_1_narrative.md`.

GDD decision: уровень сказочности для первого playable - **явная сказка без мутной магии**. Baseline строка пути: `Путь ждет первого путника.`

## Главный принцип

Игрок должен понять core loop через действие, а не через лекцию:

```text
герой выходит из аула
-> герой сам идет по дороге
-> мир дает ресурс
-> герой находит карту Саксаул
-> игрок ставит Саксаул в roadside
-> Саксаул срабатывает позже
-> круг завершается у аула
-> игрок выбирает 1 из 3
-> второй круг дает риск
-> смерть/почти смерть создает Последнюю Тамгу
-> новый наследник подбирает Тамгу
```

## Что игрок не должен делать в FTUE

- Не управлять героем WASD/стрелками/кликами по дороге.
- Не выбирать на каждой клетке.
- Не ставить первый тайл в `road_path` или `road_buffer`.
- Не открывать справочник правил.
- Не разбираться с полной прокачкой аула.
- Не видеть Оазис как первую обучающую карту.

## Что игрок должен сделать сам

1. Поставить первый Саксаул.
2. Выбрать одну карту после первого круга.
3. Поставить выбранную карту в подсвеченную зону.
4. Нажать "Новый наследник" после результата забега.

Все остальное в первые 10 минут может быть автоматическим или scripted.

## Teaching budget

Правило FTUE: **одно новое понятие на один ключевой момент**.

| Фаза | Учим | Показываем фоном | Не объясняем |
| --- | --- | --- | --- |
| A. Дом и путь | аул, герой, автодвижение | дорога, пустыня | карты, field, смерть, апгрейды |
| B. Первое влияние | Саксаул, roadside, отложенный эффект | road buffer как запрет | выбор из 3, field-экономика, риск |
| C. Первый выбор | разные роли карт, разные placement-зоны | большой field и скролл | синергии, реликты, аул-экономика |
| D. Риск и наследие | проверка, Тамга, новый наследник | сохранение ресурсов в аул | полное дерево меты |

Если состояние пытается объяснить две новые системы сразу, его нужно разделить или убрать лишний текст.

## Постоянная раскладка UI

| Зона | Что показывает | В FTUE |
| --- | --- | --- |
| Центр | карта, аул, дорога, пустыня | всегда главный фокус |
| Верх | круг, день/шаг, ресурсы забега, ресурсы аула | видим после появления карты |
| Низ | рука карт | активна только при карте/выборе |
| Слева | чат/боевой лог | короткие строки, без модалок |
| Справа | герой, Силы, статы, экипировка/пустые слоты | видим после выхода героя |

Пауза используется только для решения. Обычный ресурс, обычный урон, обычная награда и срабатывание Саксаула не ставят паузу.

## UI lock rules

| Состояние | Карты | Карта мира | Камера | Лог | Правая панель |
| --- | --- | --- | --- | --- | --- |
| `ftue_00_boot` | скрыты | скрыта | locked | скрыт | скрыта |
| `ftue_03_aul_reveal` | скрыты | view only | locked на аул | 1 строка | скрыта/пустая |
| `ftue_04_aul_exit` | скрыты | view only | locked аул -> дорога | active | герой visible |
| `ftue_06_autowalk_hint` | disabled tray | view only | soft follow | active | герой visible |
| `ftue_08_card_saxaul_found` | only Саксаул | view only | locked nearby slots | active | visible |
| `ftue_09_saxaul_place_prompt` | Саксаул selected | only valid roadside clickable | locked placement area | active | visible |
| `ftue_13_first_choice_opened` | 3 choices clickable | dimmed | locked overview | active | visible |
| `ftue_14_first_choice_place_prompt` | selected card | only valid placement clickable | may pan to field | active | visible |
| `ftue_20_run_result` | hidden | view only | aul + tamga relation | active | result panel |

Ошибочный input не открывает модалку. Он дает короткий bubble у клетки и строку в лог.

## Визуальные зоны карты

| Зона | Можно строить | Как объясняется игроку |
| --- | --- | --- |
| `aul_core` | нет | дом, старт, возврат, мета-прогресс |
| `road_path` | нет | путь героя |
| `road_buffer` | нет | кромка пути, нужна для читаемости дороги |
| `roadside_build` | да | ближние тайлы, влияют на проход |
| `field_build` | да | дальняя пустыня, ауры, доход, синергии |

Свободные `roadside_build` и `field_build` не должны выглядеть пустыми. У каждой такой клетки есть `base_decor_id`.

Первый Саксаул не требует скролла. Он ставится в видимый `roadside_build` слот. Камера большого мира раскрывается позже, когда игрок выбирает Юрту или Камень Тамги и видит `field_build`.

## Состояния FTUE

### `ftue_00_boot`

| Поле | Значение |
| --- | --- |
| Trigger | новая игра / первый запуск playable |
| Camera | черный экран |
| UI | скрыть HUD, руку карт, панель героя, лог |
| Player input | только skip intro |
| Animation | тихий песок на черном фоне |
| Text | `Песок стирает следы.` |
| Audio | ветер, низкий тон, без музыки или очень тихий дрон |
| Data mutation | нет |
| Exit | 0.8-1.0 сек прошло или игрок нажал skip/accelerate |

Задача: сразу задать тон. Никаких меню, энциклопедии, настроек, кнопок "как играть". Экран не должен быть мертвым: песок/ветер видны почти сразу.

### `ftue_01_campfire_line`

| Поле | Значение |
| --- | --- |
| Trigger | завершен `ftue_00_boot` |
| Camera | черный экран |
| UI | все еще скрыт |
| Player input | только skip intro |
| Animation | первая строка растворяется, в темноте проступает слабый огонек костра |
| Text | `Путь ждет первого путника.` |
| Audio | ветер, короткий удар бубна/струны при появлении строки |
| Data mutation | нет |
| Exit | 1.0-1.2 сек прошло или skip/accelerate |

`Род хранит имена` не выводим в первые 6 секунд. Это сильная, но абстрактная строка; ее лучше использовать позже, когда Тамга/смерть сделают память рода механикой.

### `ftue_02_desert_reveal`

| Поле | Значение |
| --- | --- |
| Trigger | завершен текст интро |
| Camera | fade-in на центр карты |
| UI | HUD еще скрыт; лог можно показать полупрозрачно после конца fade |
| Player input | нет |
| Animation | сначала огонек костра, затем силуэты юрт, потом песок и кромка дороги |
| Text/log | `Песок открывает путь.` |
| Art | песок не пустой: дюны, камни, сухая трава, следы, трещины |
| Data mutation | создать/показать карту первого playable |
| Exit | карта читаема, fade-in завершен |

Задача: показать, что это не пустая сетка. Даже до тайлов мир должен выглядеть живым. Аул не возникает магически: он был здесь, камера/тьма/песок постепенно открывают его.

### `ftue_03_aul_reveal`

| Поле | Значение |
| --- | --- |
| Trigger | пустыня появилась |
| Camera | центр на ауле |
| UI | показать верхний HUD без лишних деталей; справа пустая панель героя может быть скрыта |
| Player input | клик по костру/аулу или кнопка `В путь` |
| Animation | костер загорается и мягко пульсирует, юрты проявляются из песка/дыма |
| Text/log | `Путь ждет первого путника.` |
| Art | 2-3 юрты, костер, маленький загон/мешки, никаких дворцов |
| Data mutation | `current_hero_archetype = traveler`, `circle = 0`, `run_supplies = 0` |
| Exit | игрок нажал костер/аул/`В путь` |

Задача: аул должен ощущаться как дом и будущий мета-центр, не как декор в середине.

Это первый readiness action. Игрок подтверждает, что готов начать, но не управляет путником напрямую.

Допустимые варианты реализации:

```text
click campfire
click aul core
press Space when prompt "В путь" visible
```

Подсказка должна быть короткой и явной. Над аулом/костром появляется анимированный указатель-палец:

```text
finger pointer над костром
мягкий bounce/pulse 1 раз в 0.8-1.0 сек
подпись рядом: "Отправиться в путь"
```

Это tutorial hint, но он должен выглядеть легким, не как большая модалка. Если игрок не нажимает 2-3 секунды, костер и pointer становятся чуть заметнее. Автостарт можно включить только как fallback через 8-10 секунд, чтобы не оставлять экран мертвым.

### `ftue_04_aul_exit`

| Поле | Значение |
| --- | --- |
| Trigger | игрок подтвердил readiness action у костра/аула |
| Camera | центр держит аул и вход на дорогу |
| UI | показать правую панель героя: имя, Силы 6, пустые/заблокированные слоты |
| Player input | нет |
| Animation | герой идет от костра к `road_entry_cell`; следы остаются и быстро стираются |
| Text/log | `Первый путник выходит из аула.` |
| Art | простой путник, мешок, дорожная одежда, маленький силуэт читается на песке |
| Data mutation | `hero_state = walking_to_road` |
| Exit | герой достиг `road_entry_cell` |

Важно: герой не появляется сразу на дороге. Этот переход объясняет, что герой вышел из аула.

`ftue_04_aul_exit` уже считается gameplay-state, а не cinematic. Input locked не потому, что идет заставка, а потому что FTUE учит автодвижение. Карта, камера, герой и UI уже живые.

### `ftue_05_road_entry`

| Поле | Значение |
| --- | --- |
| Trigger | герой дошел до первой дорожной клетки |
| Camera | чуть отъехать, чтобы были видны аул, часть кольца, roadside/field |
| UI | показать `Круг 1`, Силы, Запасы, лог |
| Player input | нет |
| Animation | первая дорожная клетка мягко подсвечивается, затем подсветка гаснет |
| Text/log | `Круг 1 начат.` |
| Data mutation | `circle = 1`, `hero_route_index = 0` |
| Exit | 0.5-1.0 сек после подсветки |

### `ftue_06_autowalk_hint`

| Поле | Значение |
| --- | --- |
| Trigger | круг 1 начался |
| Camera | следует за героем так, чтобы не потерять аул из контекста |
| UI | рука карт видна, но пустая/заблокированная |
| Player input | нет; клики по карте не дают команд движения |
| Animation | герой сам проходит 2-3 клетки |
| Text/log | `Путь ведет его сам.` |
| Data mutation | герой продвигается по маршруту |
| Exit | герой дошел до scripted resource cell |

Задача: игрок должен перестать искать управление героем. Если игрок кликает по дороге, можно дать мягкую строку без модалки: `Путь уже ведет его.`

### `ftue_07_first_resource`

| Поле | Значение |
| --- | --- |
| Trigger | герой входит в scripted resource cell |
| Camera | без остановки |
| UI | HUD "Запасы" подсвечивается |
| Player input | нет |
| Animation | маленький мешок/связка припасов появляется и втягивается в HUD |
| Text/log | `Путь дает роду: Запасы +1.` |
| Data mutation | `run_supplies += 1` |
| Exit | pop-up исчез, герой продолжает движение |

Паузы нет. Это обычное событие.

### `ftue_08_card_saxaul_found`

| Поле | Значение |
| --- | --- |
| Trigger | герой входит в scripted first-card cell |
| Camera | удержать героя, дорогу и ближайшие build-зоны |
| UI | игра ставит паузу; рука карт активна; показать одну карту `Саксаул` |
| Player input | можно выбрать только `Саксаул` |
| Animation | карта выезжает снизу в руку |
| Text/log | `Песок открывает знак: Саксаул.` |
| Data mutation | `hand = [saxaul]`, `game_paused = true` |
| Exit | карта выбрана |

Карта Саксаул: низкий куст, хворост, небольшая тень. Это обычная помощь пустыни, не редкая награда.

### `ftue_09_saxaul_place_prompt`

| Поле | Значение |
| --- | --- |
| Trigger | игрок выбрал карту `Саксаул` |
| Camera | показывает выбранную карту, road buffer и 1-3 valid roadside slots |
| UI | подсветить valid `roadside_build`; затемнить `road_path`, `road_buffer`, `field_build` |
| Player input | разрешен клик только по valid roadside slot |
| Animation | valid slot пульсирует; связанная road cell тонко подсвечена |
| Text/log | `Поставь Саксаул за кромкой дороги: путник найдет тень и хворост позже.` |
| Data mutation | нет до клика |
| Exit | игрок кликает valid slot |

Ошибочный клик:

| Куда кликнул | Ответ |
| --- | --- |
| `road_path` | `Это путь героя. Здесь не строят.` |
| `road_buffer` | `Кромка пути: строить нельзя.` |
| `field_build` | `Саксаул в обучении ставится ближе к дороге.` |
| занятый слот | `Здесь уже стоит знак.` |

Ошибочный клик не открывает модалку и не снимает выбранную карту.

### `ftue_10_saxaul_placed`

| Поле | Значение |
| --- | --- |
| Trigger | valid slot выбран |
| Camera | короткий zoom/акцент на слоте |
| UI | карта исчезает из руки; пауза снимается |
| Player input | нет, пока анимация ставится |
| Animation | карта летит в слот, песок поднимается, появляется куст |
| Text/log | `В песке цепляется корнями Саксаул.` |
| Data mutation | `cell.tile_id = saxaul`, `hand.remove(saxaul)`, `linked_road_cell = nearest_or_scripted` |
| Exit | тайл стоит, linked road cell сохранена |

После постановки обязательно показать связь: короткая линия от Саксаула к дорожной клетке.

### `ftue_11_saxaul_triggered`

| Поле | Значение |
| --- | --- |
| Trigger | герой входит в `linked_road_cell` |
| Camera | без паузы; акцент на Саксаул и героя |
| UI | HUD Сил и Запасов подсвечивается |
| Player input | нет |
| Animation | линия связи вспыхивает, куст качается, над героем pop-up `Силы +1`, рядом `Запасы +1` |
| Text/log | `Саксаул: путник находит тень и хворост. Силы +1. Запасы +1.` |
| Data mutation | `hero_stamina += 1`, `run_supplies += 1` |
| Exit | pop-up исчез, герой продолжает круг |

Это самый важный момент FTUE. Игрок должен увидеть причинность:

```text
поставил карту
-> она стала тайлом
-> тайл связан с будущей клеткой
-> герой дошел туда
-> эффект сработал
```

### `ftue_12_lap_1_complete`

| Поле | Значение |
| --- | --- |
| Trigger | герой вернулся к аулу / прошел полный route |
| Camera | центр на ауле, дорога и Саксаул остаются видны |
| UI | поставить паузу после строки лога |
| Player input | пока нет, затем выбор карты |
| Animation | герой проходит рядом с аулом, костер вспыхивает чуть ярче |
| Text/log | `Круг 1 завершен. Аул снова впереди.` |
| Data mutation | `circle = 2` подготовлен, но движение еще на паузе |
| Exit | открыт выбор 1 из 3 |

### `ftue_13_first_choice_opened`

| Поле | Значение |
| --- | --- |
| Trigger | завершен первый круг |
| Camera | карта затемняется на 15-25%, аул и путь остаются читаемы |
| UI | крупный выбор 3 карт, не таблица |
| Player input | выбрать одну карту |
| Animation | три карты выходят снизу и слегка разъезжаются |
| Text/log | `Род советуется у огня: безопасность, память или риск.` |
| Data mutation | нет до выбора |
| Exit | игрок выбрал карту |

Карты выбора:

| Карта | Placement | Короткий текст на карте | Обещание игроку |
| --- | --- | --- | --- |
| `yurt` / Юрта | `field` | `Запасы +2 за стоянку.` | безопасная экономика |
| `tamga_stone` / Камень Тамги | `field` | `Мудрость +2.` | память и будущие синергии |
| `wolf_track` / Звериная тропа | `roadside` | `Риск Body, Слава +2.` | опасный маршрут ради славы |

### `ftue_14_first_choice_place_prompt`

| Поле | Значение |
| --- | --- |
| Trigger | выбрана одна из трех карт |
| Camera | зависит от placement |
| UI | подсветить только valid placement зоны |
| Player input | клик по valid slot |
| Animation | valid-зоны пульсируют разными цветами: roadside ближе к дороге, field дальше |
| Text/log | `Выбран знак: {tile}. Найди подходящее место.` |
| Data mutation | нет до клика |
| Exit | карта поставлена |

Правило подсветки:

| Выбор | Что подсветить | Что затемнить |
| --- | --- | --- |
| Юрта | 2-4 `field_build` клетки | `road_path`, `road_buffer`, `roadside_build` |
| Камень Тамги | 2-4 `field_build` клетки | `road_path`, `road_buffer`, `roadside_build` |
| Звериная тропа | 1-3 `roadside_build` клетки | `road_path`, `road_buffer`, дальний `field_build` |

Задача: на втором выборе впервые объяснить, что не все тайлы должны стоять рядом с дорогой.

### `ftue_15_second_loop_start`

| Поле | Значение |
| --- | --- |
| Trigger | выбранная карта поставлена |
| Camera | возвращается к герою |
| UI | пауза снята, рука карт может быть пустой |
| Player input | нет |
| Animation | герой продолжает путь, поставленные тайлы остаются на карте |
| Text/log | `Круг 2 начат.` |
| Data mutation | `circle = 2`, `game_paused = false` |
| Exit | герой достигает первого positive/neutral event |

### `ftue_16_second_loop_positive`

| Поле | Значение |
| --- | --- |
| Trigger | срабатывает Саксаул или выбранный безопасный/field тайл |
| Camera | без паузы |
| UI | подсветить измененный ресурс |
| Player input | нет |
| Animation | как у обычного tile trigger |
| Text/log | зависит от тайла |
| Data mutation | по `tiles.tsv` |
| Exit | герой продолжает путь |

Тексты:

```text
Саксаул: путник находит тень и хворост. Силы +1. Запасы +1.
Юрта: род получает Запасы +2.
Камень Тамги: Мудрость +2.
```

Если игрок выбрал Звериную тропу, этот state можно пропустить и перейти к риску.

### `ftue_17_first_risk_reveal`

| Поле | Значение |
| --- | --- |
| Trigger | герой подходит к scripted risk cell или к Звериной тропе |
| Camera | держит героя и опасность в кадре |
| UI | без модалки; лог получает danger-строку |
| Player input | нет |
| Animation | песок темнеет, на клетке видны следы/ветер/красноватая кромка |
| Text/log | `Песок темнеет впереди: Волчий след открыт на дороге.` |
| Data mutation | если игрок не выбрал Звериную тропу, создать временный scripted road risk |
| Exit | герой входит в risk cell |

Если игрок выбрал Юрту или Камень Тамги, риск все равно появляется, но текст должен показать, что это мир впереди, а не отмена выбора игрока.

### `ftue_18_first_check_resolved`

| Поле | Значение |
| --- | --- |
| Trigger | герой входит в risk cell |
| Camera | без отдельной боевой сцены |
| UI | справа подсветить Силы; в логе danger/success |
| Player input | нет |
| Animation | короткая вспышка проверки, герой отшатывается при провале |
| Text/log success | `Волчий след: проверка Body пройдена. Род получает Славу +2.` |
| Text/log fail | `Волчий след: проверка Body провалена. Силы -2. Род получает часть Славы.` |
| Data mutation success | `run_glory += 2` |
| Data mutation fail | `hero_stamina -= 2`, `run_glory += floor(2 * check_fail_reward_pct / 100)` |
| Exit | если `hero_stamina <= 0`, перейти к смерти; иначе к near-death/scripted pressure |

### `ftue_19_death_or_near_death`

| Поле | Значение |
| --- | --- |
| Trigger | герой умер или FTUE довело героя до состояния 1 Сила |
| Camera | стоп на клетке события |
| UI | пауза; рука карт скрыта; справа герой затемнен/ослаблен |
| Player input | только продолжить к результату |
| Animation | герой опускается/останавливается; на песке проявляется знак Тамги |
| Text/log death | `Силы иссякли. Путь окончен, но имя осталось.` |
| Text/log near death | `Путник едва стоит. На песке проступает Последняя Тамга.` |
| Data mutation | создать `last_tamga` на текущей клетке |
| Exit | Тамга видна и записана в состояние мира |

Допущение: для первого playable Тамга может появиться и при "почти смерти", если нужно гарантированно показать наследование за 10 минут. Это FTUE-допущение, не финальное общее правило.

### `ftue_20_run_result`

| Поле | Значение |
| --- | --- |
| Trigger | Тамга появилась |
| Camera | аул и место Тамги по возможности видны или связаны линией |
| UI | результат забега: собрал, сохранил в аул, потерял |
| Player input | кнопка `Новый наследник` |
| Animation | часть ресурсов летит/перетекает в сторону аула |
| Text/log | `Аул сохраняет: Запасы аула +{supplies}, Память рода +{memory}.` |
| Data mutation | `aul_supplies += banked_supplies`, `clan_memory += banked_memory`, `fallen_count += 1` |
| Exit | игрок нажал `Новый наследник` |

Результат не должен выглядеть как game over. Это итог попытки и подготовка следующей.

### `ftue_21_new_heir_exit`

| Поле | Значение |
| --- | --- |
| Trigger | игрок нажал `Новый наследник` |
| Camera | снова центр на ауле |
| UI | сбросить run HUD, сохранить aul HUD; справа новый герой |
| Player input | нет во время выхода |
| Animation | новый герой выходит от костра тем же понятным маршрутом |
| Text/log | `Новый путник выходит из аула. Костер остается за спиной.` |
| Data mutation | `current_hero_archetype = next_traveler`, `hero_stamina = 6`, `run_* = 0`, карта сохраняет поставленные тайлы и Тамгу |
| Exit | новый герой на дороге |

### `ftue_22_last_tamga_pickup`

| Поле | Значение |
| --- | --- |
| Trigger | новый герой доходит до клетки с `last_tamga` |
| Camera | акцент на Тамге и герое |
| UI | HUD Мудрости/Славы подсвечивается |
| Player input | нет |
| Animation | Тамга вспыхивает, поднимается с песка, исчезает в HUD/панели памяти |
| Text/log | `Путник поднимает Тамгу прошлого. Мудрость +1, Слава +1.` |
| Data mutation | `run_wisdom += 1`, `run_glory += 1`, remove `last_tamga` |
| Exit | Тамга исчезла, игрок видел полный core loop |

Это финальная точка FTUE MVP.

## Минимальные telemetry/funnel ids

Даже для джема полезно заложить события, чтобы вручную или автоматически видеть, где игрок теряется:

```text
ftue_intro_start
ftue_aul_revealed
ftue_aul_exit_started
ftue_road_entry
ftue_first_resource
ftue_card_saxaul_found
ftue_saxaul_selected
ftue_saxaul_placed
ftue_saxaul_triggered
ftue_lap_1_complete
ftue_first_choice_opened
ftue_first_choice_selected
ftue_first_choice_placed
ftue_first_risk_seen
ftue_first_check_resolved
ftue_first_death_or_near_death
ftue_run_result_opened
ftue_new_heir_started
ftue_last_tamga_picked
```

## Acceptance checklist

FTUE готов, если тестер без устных объяснений может сказать:

1. Герой вышел из аула, а не просто появился на дороге.
2. Герой идет сам.
3. Игрок влияет через карты и тайлы.
4. Первый Саксаул поставлен в `roadside`, не в дорогу и не в buffer.
5. Саксаул сработал позже, когда герой дошел до связанной клетки.
6. Первый круг вернул героя к аулу.
7. Первый выбор из 3 понятен по ролям: безопасность, память, риск.
8. `field` и `roadside` визуально отличаются.
9. `road_buffer` понятно запрещен для строительства.
10. Второй круг показал риск.
11. Смерть или почти смерть создала Последнюю Тамгу.
12. Результат забега сохранил часть ресурсов в аул.
13. Новый наследник вышел из аула.
14. Новый наследник подобрал Тамгу прошлого.

## Что можно резать без потери core

- Озвучку.
- Сложную анимацию песка.
- Несколько вариантов первого ресурса.
- Полный экран результата с красивой статистикой.
- Сложные реликты и экипировку.

## Что нельзя резать

- Выход героя из аула.
- Автодвижение.
- Первый Саксаул как безопасная карта.
- Ручную постановку Саксаула игроком.
- Отложенное срабатывание Саксаула.
- Различие `roadside`, `field`, `road_buffer`.
- Первый риск второго круга.
- Последнюю Тамгу.
- Нового наследника.
