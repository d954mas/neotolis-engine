# Skeleton & Pose raw inputs

The scene geometry is procedural. This directory is reserved for future
showcase-owned UI art; the current pack uses a generated white pixel and the
permitted Roboto Regular font reused from `examples/ui_showcase/raw` under
Apache License 2.0.
