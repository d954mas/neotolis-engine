fpng — Fast 24/32bpp PNG image writer/reader
=============================================

Upstream:  https://github.com/richgel999/fpng
Author:    Richard Geldreich, Jr. (same author as deps/basisu / basis_universal)
License:   The Unlicense / public-domain (banner in fpng.h:1, full text at the end of fpng.cpp)
Version:   1.0.6 (per the fpng.cpp:1 banner)

Pinned commit (vendored from main HEAD):
  925796543b9d26b8edfcdcecd94c1dac280f29fc

Files vendored (source drop — only these two, no build scripts, no postinstall):
  fpng.h    (src/fpng.h   upstream)
  fpng.cpp  (src/fpng.cpp upstream)

How obtained:
  curl https://raw.githubusercontent.com/richgel999/fpng/main/src/fpng.{h,cpp}
  commit SHA via https://api.github.com/repos/richgel999/fpng/commits/main

Notes:
  - SSE4.1 path is gated on x86/x64 detection (FPNG_X86_OR_X64_CPU). On a non-x86
    target (EMSCRIPTEN/WASM) FPNG_X86_OR_X64_CPU==0, so fpng takes the
    scalar path automatically — NO flag is needed. FPNG_NO_SSE=1 only forces the
    scalar path on an x86 host (do not add it for the WASM build — it is redundant).
  - The std::vector<uint8_t> output never crosses the C boundary; the engine/fpng
    C-API wrapper memcpy's into a caller buffer.
