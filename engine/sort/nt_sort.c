#include "sort/nt_sort.h"

/* Stub -- TDD RED phase: tests must fail against this. */
void nt_sort_by_key(nt_render_item_t *items, uint32_t count, nt_render_item_t *scratch) {
    (void)items;
    (void)count;
    (void)scratch;
}
