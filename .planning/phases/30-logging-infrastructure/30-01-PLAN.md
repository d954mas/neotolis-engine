---
phase: 30-logging-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/log/nt_log.h
  - engine/log/default/nt_log.c
  - engine/log/stub/nt_log_stub.c
  - engine/log/CMakeLists.txt
  - cmake/nt_module.cmake
  - engine/core/CMakeLists.txt
  - engine/app/CMakeLists.txt
  - engine/window/CMakeLists.txt
  - engine/input/CMakeLists.txt
  - engine/graphics/CMakeLists.txt
  - engine/fs/CMakeLists.txt
  - engine/http/CMakeLists.txt
  - engine/hash/CMakeLists.txt
  - engine/time/CMakeLists.txt
  - engine/pool/CMakeLists.txt
  - engine/entity/CMakeLists.txt
  - engine/resource/CMakeLists.txt
  - engine/material/CMakeLists.txt
  - engine/render/CMakeLists.txt
  - engine/renderers/CMakeLists.txt
  - engine/comp_storage/CMakeLists.txt
  - engine/transform_comp/CMakeLists.txt
  - engine/mesh_comp/CMakeLists.txt
  - engine/material_comp/CMakeLists.txt
  - engine/drawable_comp/CMakeLists.txt
  - tests/unit/test_log_swap.c
  - tests/unit/test_log_api.c
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08

must_haves:
  truths:
    - "nt_log_info(fmt, ...) accepts printf-style format args and prints 'INFO: message'"
    - "nt_log_warn(fmt, ...) prints 'WARN: message' to stderr on native"
    - "nt_log_error(fmt, ...) prints 'ERROR: message' to stderr on native"
    - "NT_LOG_INFO(...) macro auto-injects domain from NT_LOG_DOMAIN into output as 'INFO:domain: message'"
    - "NT_LOG_DOMAIN resolves from user #define first, falls back to CMake NT_LOG_DOMAIN_DEFAULT"
    - "Using NT_LOG_INFO without any domain defined produces a compile error with _Static_assert message"
    - "nt_log_set_level(NT_LOG_LEVEL_WARN) suppresses INFO messages"
    - "Stub backend compiles with all new function signatures as no-ops"
    - "All 37 nt_add_module() calls have LOG_DOMAIN parameter and CMake configures successfully"
  artifacts:
    - path: "engine/log/nt_log.h"
      provides: "Complete logging API: level enum, plain functions, impl functions, domain macros, format attributes"
      contains: "nt_log_level_t"
    - path: "engine/log/default/nt_log.c"
      provides: "Variadic implementation with vsnprintf, level filtering, platform routing"
      contains: "vsnprintf"
    - path: "engine/log/stub/nt_log_stub.c"
      provides: "No-op stub matching all new signatures"
      contains: "nt_log_warn_impl"
    - path: "cmake/nt_module.cmake"
      provides: "LOG_DOMAIN required parameter injection"
      contains: "NT_LOG_DOMAIN_DEFAULT"
    - path: "tests/unit/test_log_api.c"
      provides: "Unit tests for variadic formatting, domain injection, level filtering, output format"
      contains: "test_log_info_formats_printf_args"
  key_links:
    - from: "cmake/nt_module.cmake"
      to: "engine/log/nt_log.h"
      via: "NT_LOG_DOMAIN_DEFAULT compile definition -> NT_LOG_DOMAIN preprocessor resolution"
      pattern: "NT_LOG_DOMAIN_DEFAULT"
    - from: "engine/log/nt_log.h"
      to: "engine/log/default/nt_log.c"
      via: "Function declarations -> definitions"
      pattern: "nt_log_info_impl"
    - from: "engine/log/nt_log.h"
      to: "engine/log/stub/nt_log_stub.c"
      via: "Function declarations -> stub definitions"
      pattern: "nt_log_warn_impl"
---

<objective>
Build the complete variadic printf-style logging API with domain injection infrastructure.

Purpose: Replace the current 2-function non-formatting API with a 3-level variadic system that auto-tags module domains via CMake, enabling all downstream migration plans.
Output: New nt_log.h header, default and stub implementations, updated CMake infrastructure with LOG_DOMAIN on all 37 modules, and comprehensive unit tests.
</objective>

<execution_context>
@C:/projects/neotolis-engine/.claude/get-shit-done/workflows/execute-plan.md
@C:/projects/neotolis-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-logging-infrastructure/30-CONTEXT.md
@.planning/phases/30-logging-infrastructure/30-RESEARCH.md

@engine/log/nt_log.h
@engine/log/default/nt_log.c
@engine/log/stub/nt_log_stub.c
@engine/log/CMakeLists.txt
@cmake/nt_module.cmake
@tests/unit/test_log_swap.c
@tests/CMakeLists.txt
@engine/core/nt_assert.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement new logging API (header + default impl + stub + CMake)</name>
  <files>
    engine/log/nt_log.h
    engine/log/default/nt_log.c
    engine/log/stub/nt_log_stub.c
    engine/log/CMakeLists.txt
    cmake/nt_module.cmake
    engine/core/CMakeLists.txt
    engine/app/CMakeLists.txt
    engine/window/CMakeLists.txt
    engine/input/CMakeLists.txt
    engine/graphics/CMakeLists.txt
    engine/fs/CMakeLists.txt
    engine/http/CMakeLists.txt
    engine/hash/CMakeLists.txt
    engine/time/CMakeLists.txt
    engine/pool/CMakeLists.txt
    engine/entity/CMakeLists.txt
    engine/resource/CMakeLists.txt
    engine/material/CMakeLists.txt
    engine/render/CMakeLists.txt
    engine/renderers/CMakeLists.txt
    engine/comp_storage/CMakeLists.txt
    engine/transform_comp/CMakeLists.txt
    engine/mesh_comp/CMakeLists.txt
    engine/material_comp/CMakeLists.txt
    engine/drawable_comp/CMakeLists.txt
  </files>
  <read_first>
    engine/log/nt_log.h
    engine/log/default/nt_log.c
    engine/log/stub/nt_log_stub.c
    engine/log/CMakeLists.txt
    cmake/nt_module.cmake
    engine/core/nt_assert.h
    engine/core/CMakeLists.txt
    engine/app/CMakeLists.txt
    engine/window/CMakeLists.txt
    engine/input/CMakeLists.txt
    engine/graphics/CMakeLists.txt
    engine/fs/CMakeLists.txt
    engine/http/CMakeLists.txt
    engine/hash/CMakeLists.txt
    engine/time/CMakeLists.txt
    engine/pool/CMakeLists.txt
    engine/entity/CMakeLists.txt
    engine/resource/CMakeLists.txt
    engine/material/CMakeLists.txt
    engine/render/CMakeLists.txt
    engine/renderers/CMakeLists.txt
    engine/comp_storage/CMakeLists.txt
    engine/transform_comp/CMakeLists.txt
    engine/mesh_comp/CMakeLists.txt
    engine/material_comp/CMakeLists.txt
    engine/drawable_comp/CMakeLists.txt
  </read_first>
  <action>
**A. Rewrite `engine/log/nt_log.h`** (complete replacement per D-01 through D-15):

```c
#ifndef NT_LOG_H
#define NT_LOG_H

/* Log levels (ordered by severity) */
typedef enum {
    NT_LOG_LEVEL_INFO  = 0,
    NT_LOG_LEVEL_WARN  = 1,
    NT_LOG_LEVEL_ERROR = 2,
    NT_LOG_LEVEL_NONE  = 3  /* suppress all logging */
} nt_log_level_t;

/* Format attribute for printf-style type checking (per D-15, no GNU extensions) */
#if defined(__GNUC__) || defined(__clang__)
#define NT_PRINTF_ATTR(fmt_idx, arg_idx) __attribute__((format(printf, fmt_idx, arg_idx)))
#else
#define NT_PRINTF_ATTR(fmt_idx, arg_idx)
#endif

/* --- Lifecycle --- */
void nt_log_init(void);
void nt_log_set_level(nt_log_level_t level);

/* --- Plain functions (no domain, for game/example code) --- per LOG-01, LOG-06 */
void nt_log_info(const char *fmt, ...) NT_PRINTF_ATTR(1, 2);
void nt_log_warn(const char *fmt, ...) NT_PRINTF_ATTR(1, 2);
void nt_log_error(const char *fmt, ...) NT_PRINTF_ATTR(1, 2);

/* --- Domain functions (called by macros, engine-internal) --- per LOG-02 */
void nt_log_info_impl(const char *domain, const char *fmt, ...) NT_PRINTF_ATTR(2, 3);
void nt_log_warn_impl(const char *domain, const char *fmt, ...) NT_PRINTF_ATTR(2, 3);
void nt_log_error_impl(const char *domain, const char *fmt, ...) NT_PRINTF_ATTR(2, 3);

/* --- Domain resolution (per LOG-03, LOG-04) --- */
#ifndef NT_LOG_DOMAIN
    #ifdef NT_LOG_DOMAIN_DEFAULT
        #define NT_LOG_DOMAIN NT_LOG_DOMAIN_DEFAULT
    #endif
#endif

/* --- Domain macros (per LOG-02, D-14) --- */
#ifdef NT_LOG_DOMAIN
    #define NT_LOG_INFO(...)  nt_log_info_impl(NT_LOG_DOMAIN, __VA_ARGS__)
    #define NT_LOG_WARN(...)  nt_log_warn_impl(NT_LOG_DOMAIN, __VA_ARGS__)
    #define NT_LOG_ERROR(...) nt_log_error_impl(NT_LOG_DOMAIN, __VA_ARGS__)
#else
    /* per LOG-05: compile error when domain macros used without domain defined */
    #define NT_LOG_INFO(...) do { _Static_assert(0, \
        "NT_LOG_DOMAIN not defined. Either #define NT_LOG_DOMAIN \"name\" before " \
        "including nt_log.h, or add LOG_DOMAIN to nt_add_module()"); } while(0)
    #define NT_LOG_WARN(...) do { _Static_assert(0, \
        "NT_LOG_DOMAIN not defined. Either #define NT_LOG_DOMAIN \"name\" before " \
        "including nt_log.h, or add LOG_DOMAIN to nt_add_module()"); } while(0)
    #define NT_LOG_ERROR(...) do { _Static_assert(0, \
        "NT_LOG_DOMAIN not defined. Either #define NT_LOG_DOMAIN \"name\" before " \
        "including nt_log.h, or add LOG_DOMAIN to nt_add_module()"); } while(0)
#endif

#endif /* NT_LOG_H */
```

**B. Rewrite `engine/log/default/nt_log.c`** (per D-09, D-10, D-11, D-12, D-13, LOG-07):

```c
#include "log/nt_log.h"
#include <stdarg.h>
#include <stdio.h>

#ifdef __EMSCRIPTEN__
#include <emscripten/console.h>
#endif

#ifndef NT_LOG_BUF_SIZE
#define NT_LOG_BUF_SIZE 256
#endif

static nt_log_level_t s_log_level = NT_LOG_LEVEL_INFO;

void nt_log_init(void) { /* no-op, reserved for future use */ }
void nt_log_set_level(nt_log_level_t level) { s_log_level = level; }

/* Shared formatting + output helper */
static void log_write(nt_log_level_t level, const char *domain,
                       const char *fmt, va_list args) {
    static const char *const level_names[] = {"INFO", "WARN", "ERROR"};
    char msg[NT_LOG_BUF_SIZE];
    (void)vsnprintf(msg, sizeof(msg), fmt, args);

#ifdef __EMSCRIPTEN__
    char out[NT_LOG_BUF_SIZE + 64];
    if (domain) {
        (void)snprintf(out, sizeof(out), "%s:%s: %s", level_names[level], domain, msg);
    } else {
        (void)snprintf(out, sizeof(out), "%s: %s", level_names[level], msg);
    }
    switch (level) {
    case NT_LOG_LEVEL_INFO:  emscripten_console_log(out);   break;
    case NT_LOG_LEVEL_WARN:  emscripten_console_warn(out);  break;
    case NT_LOG_LEVEL_ERROR: emscripten_console_error(out);  break;
    default: break;
    }
#else
    /* per D-09: INFO -> stdout, WARN + ERROR -> stderr */
    FILE *stream = (level >= NT_LOG_LEVEL_WARN) ? stderr : stdout;
    if (domain) {
        (void)fprintf(stream, "%s:%s: %s\n", level_names[level], domain, msg);
    } else {
        (void)fprintf(stream, "%s: %s\n", level_names[level], msg);
    }
#endif
}

/* Plain functions (no domain) - per LOG-01 */
void nt_log_info(const char *fmt, ...) {
    if (s_log_level > NT_LOG_LEVEL_INFO) return;
    va_list args; va_start(args, fmt);
    log_write(NT_LOG_LEVEL_INFO, NULL, fmt, args);
    va_end(args);
}

void nt_log_warn(const char *fmt, ...) {
    if (s_log_level > NT_LOG_LEVEL_WARN) return;
    va_list args; va_start(args, fmt);
    log_write(NT_LOG_LEVEL_WARN, NULL, fmt, args);
    va_end(args);
}

void nt_log_error(const char *fmt, ...) {
    if (s_log_level > NT_LOG_LEVEL_ERROR) return;
    va_list args; va_start(args, fmt);
    log_write(NT_LOG_LEVEL_ERROR, NULL, fmt, args);
    va_end(args);
}

/* Domain functions (called by macros) - per LOG-02 */
void nt_log_info_impl(const char *domain, const char *fmt, ...) {
    if (s_log_level > NT_LOG_LEVEL_INFO) return;
    va_list args; va_start(args, fmt);
    log_write(NT_LOG_LEVEL_INFO, domain, fmt, args);
    va_end(args);
}

void nt_log_warn_impl(const char *domain, const char *fmt, ...) {
    if (s_log_level > NT_LOG_LEVEL_WARN) return;
    va_list args; va_start(args, fmt);
    log_write(NT_LOG_LEVEL_WARN, domain, fmt, args);
    va_end(args);
}

void nt_log_error_impl(const char *domain, const char *fmt, ...) {
    if (s_log_level > NT_LOG_LEVEL_ERROR) return;
    va_list args; va_start(args, fmt);
    log_write(NT_LOG_LEVEL_ERROR, domain, fmt, args);
    va_end(args);
}
```

**C. Rewrite `engine/log/stub/nt_log_stub.c`** (per LOG-08):

```c
#include "log/nt_log.h"

void nt_log_init(void) {}
void nt_log_set_level(nt_log_level_t level) { (void)level; }

void nt_log_info(const char *fmt, ...)  { (void)fmt; }
void nt_log_warn(const char *fmt, ...)  { (void)fmt; }
void nt_log_error(const char *fmt, ...) { (void)fmt; }

void nt_log_info_impl(const char *domain, const char *fmt, ...)  { (void)domain; (void)fmt; }
void nt_log_warn_impl(const char *domain, const char *fmt, ...)  { (void)domain; (void)fmt; }
void nt_log_error_impl(const char *domain, const char *fmt, ...) { (void)domain; (void)fmt; }
```

**D. Update `cmake/nt_module.cmake`** (per LOG-03, D-05):

Replace `function(nt_add_module name)` body. Use `cmake_parse_arguments(PARSE_ARGV 1 NT_MOD "" "LOG_DOMAIN" "")`. Check `if(NOT DEFINED NT_MOD_LOG_DOMAIN)` and call `message(FATAL_ERROR ...)`. Change `add_library(${name} STATIC ${ARGN})` to `add_library(${name} STATIC ${NT_MOD_UNPARSED_ARGUMENTS})`. Add `target_compile_definitions(${name} PRIVATE NT_LOG_DOMAIN_DEFAULT="${NT_MOD_LOG_DOMAIN}")`. Keep all existing lines (include dirs, warning flags, sanitizer flags, output dir, alias).

**E. Update `engine/log/CMakeLists.txt`** (per D-06, D-07):

Change `nt_add_module(nt_log ...)` to `nt_add_module(nt_log LOG_DOMAIN "log" ...)`. Change `nt_add_module(nt_log_stub ...)` to `nt_add_module(nt_log_stub LOG_DOMAIN "log_stub" ...)`.

**F. Update ALL 21 other module CMakeLists.txt files** (per D-05, D-06, D-07):

Add `LOG_DOMAIN "domain"` as second argument to every `nt_add_module()` call. Domain is module name without `nt_` prefix per D-06. Complete list:

| File | Module | Domain |
|------|--------|--------|
| `engine/core/CMakeLists.txt` | `nt_core` | `"core"` |
| `engine/app/CMakeLists.txt` | `nt_app` (x2), `nt_app_stub` | `"app"`, `"app"`, `"app_stub"` |
| `engine/window/CMakeLists.txt` | `nt_window` (x2), `nt_window_stub` | `"window"`, `"window"`, `"window_stub"` |
| `engine/input/CMakeLists.txt` | `nt_input` (x2), `nt_input_stub` | `"input"`, `"input"`, `"input_stub"` |
| `engine/graphics/CMakeLists.txt` | `nt_gfx` (x2), `nt_gfx_stub` | `"gfx"`, `"gfx"`, `"gfx_stub"` |
| `engine/fs/CMakeLists.txt` | `nt_fs` (x2), `nt_fs_stub` | `"fs"`, `"fs"`, `"fs_stub"` |
| `engine/http/CMakeLists.txt` | `nt_http` (x2), `nt_http_stub` | `"http"`, `"http"`, `"http_stub"` |
| `engine/hash/CMakeLists.txt` | `nt_hash` | `"hash"` |
| `engine/time/CMakeLists.txt` | `nt_time` | `"time"` |
| `engine/pool/CMakeLists.txt` | `nt_pool` | `"pool"` |
| `engine/entity/CMakeLists.txt` | `nt_entity` | `"entity"` |
| `engine/resource/CMakeLists.txt` | `nt_resource` | `"resource"` |
| `engine/material/CMakeLists.txt` | `nt_material` | `"material"` |
| `engine/render/CMakeLists.txt` | `nt_render` | `"render"` |
| `engine/renderers/CMakeLists.txt` | `nt_shape_renderer`, `nt_mesh_renderer` | `"shape_renderer"`, `"mesh_renderer"` |
| `engine/comp_storage/CMakeLists.txt` | `nt_comp_storage` | `"comp_storage"` |
| `engine/transform_comp/CMakeLists.txt` | `nt_transform_comp` | `"transform_comp"` |
| `engine/mesh_comp/CMakeLists.txt` | `nt_mesh_comp` | `"mesh_comp"` |
| `engine/material_comp/CMakeLists.txt` | `nt_material_comp` | `"material_comp"` |
| `engine/drawable_comp/CMakeLists.txt` | `nt_drawable_comp` | `"drawable_comp"` |

For each file: insert `LOG_DOMAIN "domain"` immediately after the module name in the `nt_add_module()` call. Example: `nt_add_module(nt_core` followed by sources becomes `nt_add_module(nt_core LOG_DOMAIN "core"` followed by sources.

**IMPORTANT**: The existing source code files still use the OLD function signatures (`nt_log_info(const char *msg)` -> now `nt_log_info(const char *fmt, ...)`). Since C allows calling a variadic function with a single string literal and no extra args (e.g., `nt_log_info("hello")` is valid for `const char *fmt, ...`), the existing call sites will continue to compile. The call site migration happens in Plan 02 and Plan 03.
  </action>
  <verify>
    <automated>cmake --build build/_cmake/native-debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `engine/log/nt_log.h` contains `typedef enum` with `NT_LOG_LEVEL_INFO`, `NT_LOG_LEVEL_WARN`, `NT_LOG_LEVEL_ERROR`, `NT_LOG_LEVEL_NONE`
    - `engine/log/nt_log.h` contains `void nt_log_warn(const char *fmt, ...) NT_PRINTF_ATTR(1, 2);`
    - `engine/log/nt_log.h` contains `void nt_log_info_impl(const char *domain, const char *fmt, ...) NT_PRINTF_ATTR(2, 3);`
    - `engine/log/nt_log.h` contains `#define NT_LOG_INFO(...)  nt_log_info_impl(NT_LOG_DOMAIN, __VA_ARGS__)`
    - `engine/log/nt_log.h` contains `_Static_assert(0,` in the no-domain fallback macros
    - `engine/log/nt_log.h` contains `void nt_log_set_level(nt_log_level_t level);`
    - `engine/log/default/nt_log.c` contains `#include <stdarg.h>` and `vsnprintf`
    - `engine/log/default/nt_log.c` contains `static nt_log_level_t s_log_level`
    - `engine/log/default/nt_log.c` contains `#ifdef __EMSCRIPTEN__` with `emscripten_console_log`
    - `engine/log/default/nt_log.c` contains `(level >= NT_LOG_LEVEL_WARN) ? stderr : stdout`
    - `engine/log/default/nt_log.c` contains output format strings `"%s:%s: %s\n"` (with domain) and `"%s: %s\n"` (without domain)
    - `engine/log/stub/nt_log_stub.c` contains `void nt_log_warn_impl(const char *domain, const char *fmt, ...)`
    - `cmake/nt_module.cmake` contains `cmake_parse_arguments(PARSE_ARGV 1 NT_MOD "" "LOG_DOMAIN" "")`
    - `cmake/nt_module.cmake` contains `NT_LOG_DOMAIN_DEFAULT="${NT_MOD_LOG_DOMAIN}"`
    - `cmake/nt_module.cmake` contains `message(FATAL_ERROR` for missing LOG_DOMAIN
    - All 37 `nt_add_module()` calls have `LOG_DOMAIN` parameter (grep confirms no `nt_add_module(` calls without `LOG_DOMAIN`)
    - `cmake --build build/_cmake/native-debug` succeeds with zero errors
    - `ctest --test-dir build/_cmake/native-debug --output-on-failure` passes all existing tests
  </acceptance_criteria>
  <done>
    New variadic logging API compiles, all 37 modules have LOG_DOMAIN injected, stub backend matches new signatures, full build passes, all existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for new logging API</name>
  <files>
    tests/unit/test_log_swap.c
    tests/unit/test_log_api.c
    tests/CMakeLists.txt
  </files>
  <read_first>
    tests/unit/test_log_swap.c
    tests/CMakeLists.txt
    engine/log/nt_log.h
    engine/log/default/nt_log.c
    engine/log/stub/nt_log_stub.c
  </read_first>
  <behavior>
    - test_log_info_formats_printf_args: `nt_log_info("value=%d", 42)` does not crash and produces output (verify function accepts variadic args)
    - test_log_warn_does_not_crash: `nt_log_warn("warning %s", "test")` completes without error (LOG-06)
    - test_log_error_formats_printf_args: `nt_log_error("code=%d", 404)` does not crash
    - test_log_domain_info: `nt_log_info_impl("gfx", "init %s", "ok")` does not crash (domain function works)
    - test_log_domain_warn: `nt_log_warn_impl("gfx", "slow %d", 1)` does not crash
    - test_log_domain_error: `nt_log_error_impl("res", "fail %d", 0)` does not crash
    - test_log_macro_info: `NT_LOG_INFO("started %d", 1)` compiles and runs (test compiled with `-DNT_LOG_DOMAIN_DEFAULT="test_mod"`)
    - test_log_macro_warn: `NT_LOG_WARN("caution %s", "x")` compiles and runs
    - test_log_macro_error: `NT_LOG_ERROR("fatal %d", 0)` compiles and runs
    - test_log_set_level_filters_info: After `nt_log_set_level(NT_LOG_LEVEL_WARN)`, `nt_log_info("hidden")` produces no output (level filtering works)
    - test_log_set_level_allows_error: After `nt_log_set_level(NT_LOG_LEVEL_WARN)`, `nt_log_error("visible")` still works
    - test_log_init_does_not_crash: `nt_log_init()` completes without error
    - test_stub_info_does_not_crash: With stub backend, `nt_log_info("test %d", 1)` is no-op
    - test_stub_warn_does_not_crash: With stub backend, `nt_log_warn("test")` is no-op
    - test_stub_domain_does_not_crash: With stub backend, `nt_log_info_impl("dom", "test %d", 1)` is no-op
  </behavior>
  <action>
**A. Update `tests/unit/test_log_swap.c`:**

Keep existing test structure but update for new variadic signatures. Add `nt_log_warn` test. All tests use plain functions (no domain -- these test executables are NOT built with `nt_add_module()` so they don't have `NT_LOG_DOMAIN_DEFAULT`).

```c
#include "unity.h"
#include "log/nt_log.h"

void setUp(void) { }
void tearDown(void) { }

void test_log_init_does_not_crash(void) {
    nt_log_init();
    TEST_PASS();
}

void test_log_info_does_not_crash(void) {
    nt_log_info("test message");
    TEST_PASS();
}

void test_log_info_formats_args(void) {
    nt_log_info("value=%d str=%s", 42, "hello");
    TEST_PASS();
}

void test_log_warn_does_not_crash(void) {
    nt_log_warn("warning %s", "test");
    TEST_PASS();
}

void test_log_error_does_not_crash(void) {
    nt_log_error("test error");
    TEST_PASS();
}

void test_log_error_formats_args(void) {
    nt_log_error("code=%d", 404);
    TEST_PASS();
}

int main(void) {
    UNITY_BEGIN();
    RUN_TEST(test_log_init_does_not_crash);
    RUN_TEST(test_log_info_does_not_crash);
    RUN_TEST(test_log_info_formats_args);
    RUN_TEST(test_log_warn_does_not_crash);
    RUN_TEST(test_log_error_does_not_crash);
    RUN_TEST(test_log_error_formats_args);
    return UNITY_END();
}
```

**B. Create `tests/unit/test_log_api.c`:**

This test file tests domain functions, domain macros, and level filtering. It is compiled with `-DNT_LOG_DOMAIN_DEFAULT="test_mod"` so the `NT_LOG_*` macros resolve.

```c
#include "unity.h"
#include "log/nt_log.h"

void setUp(void) { nt_log_set_level(NT_LOG_LEVEL_INFO); }
void tearDown(void) { }

/* Domain impl functions */
void test_log_domain_info(void) {
    nt_log_info_impl("gfx", "init %s", "ok");
    TEST_PASS();
}

void test_log_domain_warn(void) {
    nt_log_warn_impl("gfx", "slow %d", 1);
    TEST_PASS();
}

void test_log_domain_error(void) {
    nt_log_error_impl("res", "fail %d", 0);
    TEST_PASS();
}

/* Domain macros (compile with -DNT_LOG_DOMAIN_DEFAULT="test_mod") */
void test_log_macro_info(void) {
    NT_LOG_INFO("started %d", 1);
    TEST_PASS();
}

void test_log_macro_warn(void) {
    NT_LOG_WARN("caution %s", "x");
    TEST_PASS();
}

void test_log_macro_error(void) {
    NT_LOG_ERROR("fatal %d", 0);
    TEST_PASS();
}

/* Level filtering */
void test_log_set_level_filters_info(void) {
    nt_log_set_level(NT_LOG_LEVEL_WARN);
    nt_log_info("this should be filtered");
    /* If we get here without crash, filtering worked (no output to verify in unit test) */
    TEST_PASS();
}

void test_log_set_level_allows_error(void) {
    nt_log_set_level(NT_LOG_LEVEL_WARN);
    nt_log_error("this should still print");
    TEST_PASS();
}

void test_log_set_level_none_filters_all(void) {
    nt_log_set_level(NT_LOG_LEVEL_NONE);
    nt_log_info("filtered");
    nt_log_warn("filtered");
    nt_log_error("filtered");
    TEST_PASS();
}

int main(void) {
    UNITY_BEGIN();
    RUN_TEST(test_log_domain_info);
    RUN_TEST(test_log_domain_warn);
    RUN_TEST(test_log_domain_error);
    RUN_TEST(test_log_macro_info);
    RUN_TEST(test_log_macro_warn);
    RUN_TEST(test_log_macro_error);
    RUN_TEST(test_log_set_level_filters_info);
    RUN_TEST(test_log_set_level_allows_error);
    RUN_TEST(test_log_set_level_none_filters_all);
    return UNITY_END();
}
```

**C. Update `tests/CMakeLists.txt`:**

Add the new `test_log_api` test target. Link against `nt_log` and `unity`. Add `target_compile_definitions(test_log_api PRIVATE NT_LOG_DOMAIN_DEFAULT="test_mod")` so the domain macros compile. Follow the exact same pattern as existing test targets (nt_set_warning_flags, nt_set_sanitizer_flags, RUNTIME_OUTPUT_DIRECTORY, add_test). Insert after the existing `test_log_stub` block.

Also add a `test_log_api_stub` target that links against `nt_log_stub` instead of `nt_log`, using the same `test_log_api.c` source, to verify stub builds with domain macros.
  </action>
  <verify>
    <automated>cmake --build build/_cmake/native-debug && ctest --test-dir build/_cmake/native-debug -R test_log --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_log_api.c` exists and contains `test_log_domain_info`, `test_log_macro_info`, `test_log_set_level_filters_info`
    - `tests/unit/test_log_swap.c` contains `test_log_warn_does_not_crash` and `test_log_info_formats_args`
    - `tests/CMakeLists.txt` contains `add_executable(test_log_api unit/test_log_api.c)`
    - `tests/CMakeLists.txt` contains `target_compile_definitions(test_log_api PRIVATE NT_LOG_DOMAIN_DEFAULT="test_mod")`
    - `tests/CMakeLists.txt` contains `add_executable(test_log_api_stub unit/test_log_api.c)`
    - `ctest --test-dir build/_cmake/native-debug -R test_log_real --output-on-failure` exits 0
    - `ctest --test-dir build/_cmake/native-debug -R test_log_stub --output-on-failure` exits 0
    - `ctest --test-dir build/_cmake/native-debug -R test_log_api$ --output-on-failure` exits 0
    - `ctest --test-dir build/_cmake/native-debug -R test_log_api_stub --output-on-failure` exits 0
    - All tests in full suite pass: `ctest --test-dir build/_cmake/native-debug --output-on-failure` exits 0
  </acceptance_criteria>
  <done>
    All log tests pass: plain variadic functions, domain impl functions, domain macros, level filtering, and stub backend. Both real and stub implementations tested with domain macros.
  </done>
</task>

</tasks>

<verification>
1. `cmake --build build/_cmake/native-debug` completes with zero errors -- proves all 37 modules have LOG_DOMAIN and new API compiles
2. `ctest --test-dir build/_cmake/native-debug -R test_log --output-on-failure` -- all log tests pass
3. `ctest --test-dir build/_cmake/native-debug --output-on-failure` -- full suite passes (no regressions)
4. Grep verification: `grep -r "nt_add_module(" engine/ --include="CMakeLists.txt" | grep -v LOG_DOMAIN` returns empty (all modules have LOG_DOMAIN)
5. Formatting: `clang-format --dry-run --Werror engine/log/nt_log.h engine/log/default/nt_log.c engine/log/stub/nt_log_stub.c`
</verification>

<success_criteria>
- New 3-level variadic logging API (INFO, WARN, ERROR) compiles and works
- Domain macros auto-inject module names into log output
- Level filtering suppresses messages below threshold
- Stub backend matches all new signatures
- All 37 nt_add_module() calls have LOG_DOMAIN parameter
- CMake FATAL_ERROR fires if LOG_DOMAIN is omitted
- All tests (existing + new) pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-logging-infrastructure/30-01-SUMMARY.md`
</output>
